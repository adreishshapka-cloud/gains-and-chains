/**
 * Координаты по макету фона.
 *
 * Фон — не картинка позади интерфейса, а сам интерфейс: рамки, панели и кнопки
 * на нём нарисованы. Движок рисует поверх только то, что меняется, и попадает
 * в нарисованные места по этим числам. Все они сняты с координатной сетки
 * (`python tools/grid_overlay.py фон.png`), а не подобраны на глаз.
 *
 * Если фон перерисуют — сетку накладывают заново и правят только этот файл.
 */

/** Размер макета. Сцена работает в этих координатах один в один. */
export const STAGE_W = 1609;
export const STAGE_H = 918;

/** Прямоугольник: x, y, ширина, высота. */
export type Rect = readonly [number, number, number, number];

/** Игровое поле внутри нарисованной рамки барабанов. */
export const REELS_AT = { x: 475, y: 105, cell: 136 } as const;

/**
 * Логотип. Прямоугольник обмерен по чернилам старого заголовка, впечатанного
 * в макет: файл логотипа — та же картинка, и в этих координатах он ложится
 * на неё буква в букву. Менять числа без перезамера нельзя — при любом другом
 * размере старая надпись начинает просвечивать в просветах между буквами.
 */
export const LOGO_AT = { x: 24, y: 24, w: 413 } as const;

/**
 * Фигура маскота (VAN).
 *
 * Задаётся в полный рост: нижняя треть уходит под панель баланса, которую
 * Game рисует поверх маскота (см. PANEL_TOP). Так и было задумано на макете —
 * персонаж стоит на полу комнаты, а панель висит перед ним.
 *
 * Ширина и высота держат пропорцию файла (598x1462): спрайт вырезан по фигуре,
 * растягивать его по осям не нужно. Исходник крупнее места на сцене, движок
 * его уменьшает — при уменьшении картинка только выигрывает в чёткости.
 *
 * Размер подобран по кадру: видно от макушки до бёдер, ниже начинается панель.
 * Крупнее — кулак закрывает кружку на тумбе и фигура обрезается по пояс;
 * мельче — теряется присутствие. Левый край сдвинут правее центра стены,
 * чтобы голова не наезжала на табличку.
 */
export const VAN_AT = { x: 95, y: 210, w: 315, h: 770 } as const;

/** Верхняя кромка панели баланса: с неё начинается слой поверх маскота. */
export const PANEL_TOP = 676;

/**
 * Трон BILLY в бонусной комнате — тот же слот маскота, где в основной стоит VAN.
 *
 * На бонусном макете трон занимает половину экрана и оказывается ровно
 * за рамкой барабанов, поэтому в комнату он попадает не фоном, а спрайтом,
 * и в слоте маскота как раз к месту по размеру.
 *
 * Шире VAN_AT: у трона к фигуре добавлены подлокотники, и в ширину фигуры
 * он садится по плечи. Низ на 694 — на два десятка ниже кромки нижней полосы,
 * чтобы подножие трона уходило под неё, а не стояло на ней.
 */
export const BOSS_AT = { x: 20, y: 255, w: 400, h: 439 } as const;

/**
 * Створка двери на сцене входа в подземелье (dungeon-entrance.png).
 *
 * Прямоугольник тот, который створка занимает ЗАКРЫТОЙ. Открываясь внутрь,
 * она из него не выходит ни на пиксель — поэтому все шестнадцать кадров
 * нарезаны в один размер и кладутся в одно место, без пересчёта координат.
 *
 * Числа посчитаны tools/prep_dungeon_entrance.py при сборке кадров: правя
 * там границы створки, эти надо переписать его выводом, а не подгонять руками.
 */
export const DOOR_AT = { x: 713, y: 148, w: 200, h: 436 } as const;

/** Раскладка листа кадров двери: 4x4, слева направо и сверху вниз. */
export const DOOR_SHEET = { cols: 4, rows: 4 } as const;

/**
 * Поле монетного бонуса (OIL RUSH) — 5×5 в своей рамке.
 *
 * Рамка собрана из рамки барабанов (tools/prep_coin_field.py) и по высоте
 * повторяет её один в один: 5 клеток по 107 плюс две кромки по 34 дают 603,
 * ровно столько же занимает рамка барабанов от 80 до 682. Поэтому поле встаёт
 * в тот же вырез комнаты, а по ширине оказывается у́же — монеты не барабаны,
 * и путать их не надо.
 *
 * Числа печатает сам скрипт при сборке рамки: правя размер клетки там,
 * эти переписывают его выводом.
 */
export const COIN_FIELD = { x: 514, y: 80, cell: 107, edge: 34, size: 603 } as const;

/**
 * Табличка «VAN / DUNGEON MASTER» на стене — готовый файл, кладётся спрайтом.
 * Старая табличка с макета снята целиком (вместе с рамкой и подзаголовком:
 * подзаголовок на новой другой), её место замощено кладкой.
 *
 * Ширина подобрана по кадру: шире — правый край уходит под голову маскота.
 * Высота считается из пропорции файла.
 */
export const SIGN_AT = { x: 40, y: 140, w: 176 } as const;

/**
 * Натюрморт на тумбе: канистра и кружка «#1». Тоже готовый файл.
 * На макете тумба стояла вплотную к прежней фигуре, и замощение снесло
 * её содержимое вместе с ней; возвращается только сама столешница,
 * а посуда ставится сверху спрайтом.
 *
 * `y` — линия, на которой предметы стоят (низ спрайта), а не его верх:
 * так их можно менять на другие, не пересчитывая высоту.
 *
 * Ширина ограничена не тумбой, а фигурой: VAN стоит вплотную и его кулак
 * висит ниже, чем висела рука прежнего маскота. В натуральный размер (150)
 * кружка уходила за кулак почти целиком, и наружу торчала только ручка —
 * читалось как деталь, повисшая в воздухе. На 105 обе вещи целиком слева
 * от фигуры.
 */
export const TABLE_AT = { x: 24, y: 648, w: 105 } as const;

/**
 * Готовые панели нижней полосы из набора: деньги и степпер ставки.
 * Ложатся поверх нарисованных на макете, прямоугольники обмерены по их рамкам.
 */
export const MONEY_PANEL = { x: 15, y: 682, w: 496, h: 103 } as const;
export const STEPPER_PANEL = { x: 528, y: 684, w: 232, h: 100 } as const;

/**
 * Значения в нижней панели — по ЦЕНТРУ своих ячеек на новых панелях
 * (раньше это был левый край строки на плоской полосе). Координаты
 * пересчитаны из ячеек ассета: панель кладётся в MONEY_PANEL/STEPPER_PANEL,
 * и центры ячеек переезжают вместе с ней.
 */
export const MONEY = {
  balance: { x: 112, y: 749 },
  bet: { x: 266, y: 749 },
  win: { x: 420, y: 749 },
  /** Число между «−» и «+», ровно по центру окна степпера. */
  stepper: { x: 646, y: 736 },
} as const;

/** Монета рядом с суммой баланса: сторона значка и зазор до цифр. */
export const COIN = { size: 22, gap: 5 } as const;

/**
 * Правая панель «ПОДЗЕМЕЛЬЕ» — готовый файл из набора.
 *
 * Раньше её нутро стиралось с макета целиком и обе колонки рисовал движок.
 * Теперь панель приходит нарисованной, вместе с подписями, разделителями,
 * рамками ячеек и таблицей выплат. Движку остаётся пять значений, которые
 * действительно меняются.
 *
 * Постоянное намеренно оставлено запечённым: выплаты символов, «Линий 20»
 * и строка горячих клавиш в игре ровно такие же, как в файле, и перерисовывать
 * их движком значило бы держать вторую копию тех же чисел.
 */
export const INFO_PANEL = { x: 1176, y: 140, w: 293, h: 448 } as const;

export const INFO = {
  /** Правый край ячеек: числа выравниваются по нему вправо. */
  valueRight: 1433,
  /** Середина строки каждой ячейки по вертикали. */
  rows: {
    tokens: 219,
    sticky: 245,
    dry: 271,
    bet: 340,
    buy: 365,
  },
  size: 17,
  /**
   * Предельная ширина значения. Ячейка нарисована под «1 280», а покупка
   * бонуса на максимальной ставке стоит 6 400 000 — семь цифр с пробелами
   * вылезают за рамку и наезжают на значок монеты слева. Что не влезло,
   * движок ужимает по ширине.
   */
  maxWidth: 56,
} as const;

/**
 * Кликабельные зоны поверх нарисованных кнопок.
 *
 * Прямоугольники совпадают с самими кнопками пиксель в пиксель, а не сидят
 * внутри них с запасом. Раньше зоны были у́же и ниже; подсветка рисуется
 * по границе зоны, и её рамка ложилась поперёк кнопки, не совпадая с краем.
 * Для ряда с готовыми рисунками числа те же, что в ROW_BUTTONS.
 */
export const BUTTONS: Record<string, Rect> = {
  // «−» и «+» обмерены по кнопкам новой панели степпера, а не по макету.
  betDown: [549, 712, 46, 49],
  betUp: [696, 712, 44, 49],
  buy: [786, 702, 164, 76],
  turbo: [962, 700, 108, 74],
  auto: [1075, 700, 98, 74],
  // На месте бывшей кнопки «ПРАВИЛА» теперь «МЕНЮ» — правила открываются
  // из «?», а дыру в ряду кнопок оставлять было некрасиво.
  menu: [1178, 700, 116, 74],
  // Плита SPIN вместе с рамкой: обмерено по её краям, а не по золотому полю.
  spin: [1318, 700, 282, 90],

  // Нижний ряд иконок — по рамкам самих кнопок (обмерено: они начинаются
  // с y=832 и кончаются на y=900, шаг между кнопками ровно 99–100).
  settings: [36, 832, 66, 68],
  sound: [135, 832, 67, 68],
  music: [235, 832, 66, 68],
  fullscreen: [331, 832, 65, 68],
  help: [425, 832, 65, 68],
  topUp: [519, 832, 66, 68],
};

/**
 * Правая половина нижней полосы одним рисунком: накопитель «DUNGEON TICKET»,
 * «OIL UP» и шкала «VAN'S FIST».
 *
 * Именно все три вместе, а не накопитель отдельно. Отдельной плашкой он
 * читался наклейкой поверх полосы: у него была своя рамка, свой тон подложки
 * и свои поля, не совпадающие с соседями. Здесь секции нарисованы на общем
 * фоне, с общими перемычками и заклёпками.
 *
 * По высоте картинка чуть вытянута против места на макете (181 против 100
 * при ширине 1262 против 868) и слегка ужимается. Заметно это только на
 * заклёпках, а альтернатива — либо щели по краям секции, либо вылет полосы
 * за нижнюю кромку экрана.
 */
export const BOTTOM_BAR = { x: 586, y: 815, w: 868, h: 100 } as const;

/**
 * Ряд кнопок «ТУРБО / АВТО / МЕНЮ» готовыми рисунками поверх макета.
 *
 * На месте «МЕНЮ» на макете была кнопка «ПРАВИЛА», и её приходилось
 * пересобирать вручную из краёв соседней «ТУРБО» с растяжкой середины —
 * выходила пустая болванка, которую движок подписывал текстом. В наборе
 * кнопка нарисована целиком, и болванка просто закрывается ею.
 *
 * Соседние две берутся заодно, и это не лишняя работа: новая кнопка рядом
 * с двумя запечёнными в макет читается чужой — у неё теплее рамка и другой
 * рисунок заклёпок. Либо все три новые, либо ни одной.
 *
 * Прямоугольники — по нарисованным кнопкам, а не по кликабельным зонам:
 * зоны у́же, они сидят внутри рамок.
 */
export const ROW_BUTTONS = {
  /** Покупка бонуса. На макете кнопка другая — здесь неоновая из набора. */
  buy: { x: 786, y: 702, w: 164, h: 76 },
  turbo: { x: 962, y: 700, w: 108, h: 74 },
  auto: { x: 1075, y: 700, w: 98, h: 74 },
  menu: { x: 1178, y: 700, w: 116, h: 74 },
} as const;

/**
 * Три билета накопителя. Координаты — по самим билетам внутри BOTTOM_BAR
 * (в файле они на x 66..144, 151..232, 241..320, y 80..130), пересчитанные
 * в экранные. Рисует билеты панель; движок только притеняет
 * несобранные и подсвечивает собранные — поэтому прямоугольники обязаны
 * совпадать с билетами точно, иначе обводка съезжает и билет вылезает за неё.
 */
export const BELT_SLOTS: readonly Rect[] = [
  [631, 859, 54, 28],
  [690, 859, 56, 29],
  [752, 859, 54, 28],
];

/**
 * Секция «OIL UP» — отдельная готовая панель поверх нижней полосы.
 *
 * На самой полосе эта секция тоже нарисована, но со своей бедой: счётчик там
 * запечён цифрой, и, чем её ни гаси, на ровной чёрной подложке видно место,
 * где гасили. В наборе секция пришла отдельным файлом с чистой ячейкой —
 * им и накрывается нарисованная.
 *
 * Прямоугольник обмерен по секции в BOTTOM_BAR: в файле полосы она занимает
 * x 390..700 из 1262 и всю высоту, что на сцене даёт эти числа. Панель кладётся
 * с перекрытием перемычек по краям — свои у неё нарисованы такие же.
 */
export const OIL_PANEL = { x: 854, y: 819, w: 213, h: 91 } as const;

/**
 * Счётчик цепей внутри этой панели. Центр прямоугольника совпадает с центром
 * запечённой в неё единицы (x 1136..1201, y 411..550 из 1977x795) — движок
 * пишет своё число ровно на её место.
 */
export const OIL_BAR: Rect = [950, 858, 60, 32];

/**
 * Экраны, пришедшие из набора готовыми картинками: правила и настройки.
 *
 * Размер каждой подобран по её же пропорции — картинка вписывается в высоту
 * сцены с полем сверху и снизу, растяжения нет. Внутренние координаты (кнопки,
 * строки статистики, ползунок) живут в файлах самих экранов и заданы в системе
 * картинки: так их снимают линейкой с исходника и не пересчитывают заново,
 * если панель на сцене поедет.
 */
export const RULES_SCREEN = { x: 129, y: 19, w: 1351, h: 880 } as const;
export const MENU_SCREEN = { x: 387, y: 14, w: 835, h: 890 } as const;
