import { LINES } from '../core/types';

/**
 * Настройки самой игры, а не математики.
 *
 * Вынесено из Game.ts, чтобы экраны интерфейса могли рассказывать о правилах,
 * не подтягивая за собой всю сцену: настройкам нужно знать, сколько раундов
 * крутит автоспин, но не нужен ни Pixi, ни мат-модель.
 */

/** Уровни ставки в монетах. Нижний равен числу линий — то есть монета на линию. */
export const BET_LEVELS = [LINES, 40, 100, 200, 400, 1000] as const;

/** Сколько раундов запускает автоспин за одно нажатие. */
export const AUTO_ROUNDS = 25;

/** Подтверждённый симулятором возврат — показывается в правилах. */
export const RTP_LABEL = '95.9%';
